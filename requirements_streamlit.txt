# Streamlit App Requirements for OncoPilot MCP System

# Core Streamlit and Web Interface
streamlit>=1.28.0
pandas>=2.0.0

# Async and MCP Support
asyncio
motor>=3.3.0  # MongoDB async driver

# MCP Framework (Install separately)
# pip install mcp

# Data Processing and JSON
bson>=0.5.10

# Optional: For enhanced functionality
httpx>=0.24.0  # For HTTP requests in MCP servers
python-dotenv>=1.0.0  # For environment variables

# Development and Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Note: FastMCP and standard MCP need to be installed separately:
# pip install mcp
# For FastMCP support, check the official MCP Python SDK documentation 